import { NearBindgen, near, call, view } from 'near-sdk-js'

@NearBindgen({})
class Counter {
  val: number = 0;

  // TODO 1 Use view to get the counter value.
  
  // TODO 2 Use call to increment the counter value by 1.
  
  // TODO 3 Use call to decrement the counter value by 1.
  
  // TODO 4 Use call to reset the counter value to 0.
}